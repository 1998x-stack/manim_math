# 小学一年级动画代码：已确认问题与分层验收记录

日期：2026-09-23。范围：`小学/一年级/上册/`、`小学/一年级/下册/`。本记录明确区分**已确认源代码缺陷**、**静态审查覆盖范围**和**尚需渲染验证的风险**。独立修复分支：`fix/grade-one-counting-hundred-chart`。

## 1. 已确认问题及本次源码修复

| 严重度 | 来源与证据 | 原因 | 本次改动与复核点 |
| --- | --- | --- | --- |
| P0 | `上册/第一章-10以内数的认识/001数一数/counting_animation.py`，原 `show_counting_demo()` | 数字加入列表后，`hide_current_number()` 立即取 `[-1]`，淡出的变成刚创建的**当前数字**，上一数字未正确替换。 | 用 `_show_counting()` 统一 5 个苹果和 6 颗星星的计数；上一数字经 `ReplacementTransform` 转为下一数字，最后只淡出当前数字。验证每帧可见标签和物品一一对应。 |
| P1 | 同文件原自定义 `Star` | 原顶点角度为 `i*4π/5`，内角 `(i+0.5)*4π/5`；交替后的角度并非逐次前进 `π/5`，部分内外顶点在同一射线上，轮廓不符合普通填充五角星。 | 新 `star_vertices()` 返回沿圆周每次增加 `π/5` 的交替内外顶点，增加半径约束与纯数学回归测试。 |
| P1 | 同文件 `setup_geometry()` | 第一列中心 `x=-4`，接近宽度 9 的画面左边界 `-4.5`；高亮轮廓可能贴边，跨平台描边有被裁切风险。 | 物品网格改为 `x=-3.2,-1.6,0,1.6,3.2`，预留边距；低清渲染仍需检查文字与高亮。 |
| P0 | `下册/第二章-100以内数的认识/003百数表/003_百数表.py` 原 `scene_3_row_pattern()`、总结卡片、片尾 | 表格按 1～10、11～20 等排布，却显示“同一行十位相同”，甚至称 11～20 十位都是 1、41～50 十位都是 4，数学上错误。 | 保留 1～100 排列，将横向规则改为相邻 +1，并用不同颜色强调行末整十数；同步修复 `description.json`、`storyboard.md`、总结文案。 |

本次**未修改**既有 `.mp4`、`*_finish.mp4`、作者标识或课程文件路径；在渲染验证以前，它们不能视为与修复后源码一致。旧 `prompt.md` 按历史原始资料保留，其数学结论与当前审定分镜冲突时不得机械复制。

## 2. 实际审查覆盖与尚未确认的风险

- 已检查：一年级上、下册章节目录；数一数原源码和分镜；百数表原源码、课程描述和分镜；整时与半时钟表核心角度构造的源码片段；仓库 `AGENTS.md`、场景技能和质量门禁。
- 尚未逐文件完成人工数学审阅：其余知识点的题干、推导、每帧数物对应和边界情况。新增的 `tools/audit_grade_one.py` 会在仓库本地/CI 遍历目录内**全部 `.py` 文件**，输出每文件语法及识别到的 Scene 类，并提示直接把中文放入 `MathTex`/`Tex` 的字面参数、缺少配套文档等；这些静态项目不代表数学语义已经合格。
- 尚未执行：目标场景的真实 Manim 渲染、中文字体可用性检查、9:16 关键帧视觉审阅、生产 MP4 更新、`ffprobe` 时长与编码审核。未获得这些证据之前，不应合并宣称“所有一年级课程已修好”或同步更新画廊成片。

## 3. 不同课程内容的针对性回归矩阵

| 区域 | 数学断言（针对现存每个 Scene 核对） | 需要实际检查的画面行为 |
| --- | --- | --- |
| 上册：10 以内数的认识与加减法 | 0 的含义；数物一一对应；第几和几个的区别；分与合的数量守恒；等号/大小号方向不反。 | 被点数物体一次只高亮一个；移走物体后数量和式同步变化；不让上一幕数字遗留。 |
| 上册：图形、20 以内数及进位加法 | 不用大小或朝向定义图形类别；19→20 的位值变化正确；凑十拆分量之和等于原量。 | 个位与十位色彩/位置一致；动态移动后位置和数字仍对应；不误将物体增减为凑十。 |
| 下册：百以内数和加减法 | 1～100 的位值、整十边界、加减逆运算及借位/不借位结果正确。 | 数表每格一数；所有例题结果、数块与字幕同时一致。 |
| 下册：认识时间 | 分针 12 对应整时、6 对应半时；8:30 时针在 8 和 9 之间；12:30 时针在 12 和 1 之间。 | 两针长度/颜色有别，旋转方向正确；数字时间与钟面一致，标题不挡住钟表。 |
| 下册：人民币 | 1 元＝10 角、1 角＝10 分；金额计算优先转为整数分，找零非负且等式成立。 | 纸币/硬币标签不冒充法定实物图案；兑换前后总额守恒，单位文字明确。 |
| 下册：几何实践与整理 | 位置关系以观察方向为前提；分类的判据一致；综合题不跨年级偷换定义。 | 不遮挡关键形状或答案；每次转场清理已失效标记，保证结论只对应当前画面。 |

表中针对其余知识点的是**下一轮验收标准，而不是声称已发现对应代码存在这些缺陷**。

## 4. 自动检查、视觉检查和合并门槛

```bash
# 静态盘点：全部现有一年级 Python 文件（会逐文件输出 Scene / 语法状态）
python tools/audit_grade_one.py --json > grade-one-source-audit.json

# 可离线运行的数学/扫描器回归测试
python -m unittest discover -s tests -p 'test_grade_one_*.py' -v

# 专项源文件的两段低清渲染；必须在具备 Manim CE + 中文字体的环境中执行
manim -pql '小学/一年级/上册/第一章-10以内数的认识/001数一数/counting_animation.py' CountingAnimation
manim -pql '小学/一年级/下册/第二章-100以内数的认识/003百数表/003_百数表.py' HundredChartLesson
```

新增 `.github/workflows/grade-one-quality.yml` 只稀疏检出一年级源码、知识点配套文档和测试文件（不下载大型 MP4），为所有 `.py` 执行静态盘点并运行上述纯数学测试。CI 通过只表示静态和列举的数学断言通过。**合并前**还需提供两个修改场景的渲染日志与首屏、计数切换、行末 20/50/100 高亮、片尾的关键帧检查结果；如发现字体、布局或对象生命周期问题，先修复再发布新视频。

后续逐课扩展时优先给每个知识点增添纯数学函数测试，动画只负责时间编排；任何数学规格与历史生成提示词冲突，保留原始提示词并在该课 `storyboard.md`/`description.json` 中记录经核验的新规格。
