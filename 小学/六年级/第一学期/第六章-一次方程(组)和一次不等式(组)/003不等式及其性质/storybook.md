# 不等式及其性质

## 概述
认识不等号(>, <, ≥, ≤)及不等式的概念。理解不等式的基本性质:①不等式两边都加上(或减去)同一个数,不等号方向不变;②不等式两边都乘以(或除以)同一个正数,不等号方向不变;③不等式两边都乘以(或除以)同一个负数,不等号方向改变。性质③是重点和难点。

## 数学公式
- 若 a > b, 则 a ± c > b ± c
- 若 a > b, c > 0, 则 ac > bc
- 若 a > b, c < 0, 则 ac < bc (方向改变)

## 相关知识点
- 不等式
- 不等号
- 不等式性质
- 方向改变
