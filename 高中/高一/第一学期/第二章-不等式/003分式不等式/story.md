# 分式不等式

## 概述
分式不等式形如f(x)/g(x)>0（或<0等）。解法要点：①不能直接交叉相乘（不知道g(x)的正负）；②将不等式化为标准形式，移项通分使一边为0；③转化为f(x)·g(x)>0（乘以g(x)²恒正）或用「数轴标根法」求解。注意分母不能为0的限制条件。

## 数学公式
- f(x)/g(x) > 0 ⟺ f(x)·g(x) > 0
- f(x)/g(x) ≥ 0 ⟺ f(x)·g(x) ≥ 0 且 g(x) ≠ 0
- 数轴标根法：奇穿偶不穿

## 相关知识点
- 分式
- 因式分解
- 数轴标根法
- 取值范围
