# Storyboard — A-Level P1 §1.2 Completing the square

- Source: supplied Cambridge P1 Coursebook (2018) §1.2, printed pp. 6–9 / PDF pp. 18–21. Original on-screen examples.
- Audience/prerequisites: learner after §1.1; factorisation, perfect-square expansions, real numbers, coordinate graph basics.
- Learning objective: transform a quadratic to vertex form, solve via `±`, interpret vertex/real roots and handle `a ≠ 1`.
- Frame: 9:16 logical, 1080×1920 target (actual resolution subject to CLI and ffprobe); safe x∈[-4,4], y∈[-7,7]. Dark navy; A-Level P1 header/footer persist throughout. On-screen Mandarin explanatory text and pure-math LaTeX separate; no external audio, textbook images, or music.

| Shot / method | Core claim / proof or test | Screen object lifecycle | Keyframe target | Approx. time |
|---|---|---|---|---:|
| 1 opening | State example identity, validated by expansion | header create persistent; title/formula/arrow create, then remove | main source/answer inside safe area | 7–10 s |
| 2 reverse expansion | `(x+3)^2=x²+6x+9`; adding and subtracting 9 preserves equality | expression rows create sequentially, then remove | all three derivation rows legible | 11–14 s |
| 3 tiled square | at x=2, 4+6+6+5=25-4=21 (positive integer example) | tile categories create sequentially, holes remain outlined; then remove | 4 missing cells visibly distinct; text agrees with model | 10–13 s |
| 4 solve | `(x+3)^2=4` yields both `x=-5,-1` | six displayed algebra steps appear and leave | ± and both roots readable | 12–15 s |
| 5 parabola | `y=(x+3)^2-4`, vertex (-3,-4), zeros -5,-1 | actual axes/plot/vertex/zero markers from same model; then remove | points/labels match actual mapped coordinates | 10–13 s |
| 6 non-monic | `2x²-8x+3=2(x-2)^2-5` | algebra rows, warning panel; then remove | negative 4 correctly multiplied by 2 | 9–12 s |
| 7 exceptional outcomes | positive square +4 >0; negative square+4 ≤4 | distinct inequality cards then remove | real-root and maximum statements carefully scoped | 8–11 s |
| 8 practice/recap | `x²-4x-1=0 ⇒ x=2±√5` | quiz, pause, solution, recap | exact two roots; no source exercise quoted | 10–14 s |

## Object lifecycle and traceability

- `brand_group`: create at opening; persist through every shot; never transition out.
- `diagram` uses one deterministic `square_completion_tiles()` model; on-screen `base`,`strips`,`corner`,`missing` and count labels all derive from the same parameters x=2 and 3-unit strip. Draw categories in deterministic order; remove after shot.
- `parabola`: graph generated by `(x+3)**2-4` and dots/labels by `parabola_vertex_and_roots()`; mapped through `axes.c2p`, with no separately guessed screen-coordinate version of the roots.
- Transitions: each shot's temporary objects removed by `wipe`; no obsolete `ReplacementTransform` references; no zero-width `Axes.plot` range; no dynamic updater.
- `checkpoint`: after shot's settled conclusion, checks that registered actual Mobjects are present and fully inside safe rectangle. No claim that a few snapshots prove all intermediate frames or no overlapping labels.

## Verification register

| Gate | State | Evidence |
|---|---|---|
| Mathematical proof | pass | math_spec.md identities and explicit domains |
| Syntax | pass | `python -m py_compile lesson.py math_model.py test_math.py` |
| Lesson unit tests | pass | `python -m unittest -v test_math.py`, five tests |
| Skill AST scan | not_run | Skill scanner unavailable locally |
| Actual Scene runtime checkpoints | blocked | Manim package unavailable; statements coded, not executed |
| Manim preview/final render | blocked | Manim package unavailable |
| Visual frame review | not_run | No new MP4 rendered |
| ffprobe / audio review | not_run | No new MP4 or audio produced |
