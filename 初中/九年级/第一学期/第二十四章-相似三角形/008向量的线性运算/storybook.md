# 向量的线性运算

## 概述
向量的线性运算包括加法、减法和数乘。任意向量都可以表示为基向量的线性组合。在平面内，取两个不共线的向量→e₁和→e₂作为基底，则平面内任一向量→a都可以唯一表示为→a=λ₁→e₁+λ₂→e₂。

## 数学公式
- →a = λ₁→e₁ + λ₂→e₂（→e₁, →e₂不共线）
- 两向量平行：→a ∥ →b ⟺ →a = λ→b（λ ∈ R）

## 相关知识点
- 基向量
- 向量分解
- 平行向量
