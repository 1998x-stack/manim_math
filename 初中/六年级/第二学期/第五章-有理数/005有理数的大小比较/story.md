# 有理数的大小比较

## 概述
有理数比较大小的方法：(1)正数大于0，负数小于0，正数大于任何负数；(2)两个正数比较绝对值，绝对值大的数大；(3)两个负数比较绝对值，绝对值大的数反而小；(4)利用数轴，右边的数大于左边的数。

## 数学公式
- 正数 > 0 > 负数
- 若 a>0, b>0, |a|>|b|, 则 a>b
- 若 a<0, b<0, |a|>|b|, 则 a<b

## 相关知识点
- 数轴
- 绝对值
- 不等式
