# 003 数轴｜逐镜数学规格与验收

入口：`number_line_lesson.py::NumberLineLesson`。画幅：9:16，1080×1920。课程重点：原点、正方向、单位长度；有理数在数轴上的表示；右大左小。竖屏关键区域建议 x∈[-4,4]、y∈[-7,7]，需要在实际渲染中核验 Mobject 包围盒。保留现有 MP4、音轨和原 prompt.md。

| 镜头 | learning_fact / 给定条件 | 屏幕状态与数据关联 | 对象生命周期 | check |
|---|---|---|---|---|
| 1 引入 | 待排序的 -3、0、1/2、2 | MathTex 分别显示四个数；此时不暗示数轴已经完备 | 临时数字淡出，作者信息全程保留 | 字形完整、数值正确 |
| 2 直线 | 尚未规定三要素时只是普通直线 | 普通 `Line` 位于 y=1.5 | `basic_line` 保留至第五镜 | 不把普通线提前称为完整数轴 |
| 3 原点 | 原点规定为 0 | 点在直线中央，标签 0 位于点下 | `origin_dot`/`origin_label` 保留；标题淡出 | 原点位置与后续 `NumberLine.n2p(0)` 一致 |
| 4 正方向 | 通常向右为正 | 箭头从左向右，旁注明确通常规定 | 临时箭头在镜头结束时清理 | 箭头方向正确，直线不被临时箭头覆盖 |
| 5 单位长度 | x_range=[-4,4,1]，7 场景单位覆盖 8 个数值单位 | `ReplacementTransform` 先将基础直线替换为 `NumberLine`，然后所有刻度、原点与 `Brace` 从该轴求坐标 | `number_line`、`tick_labels`、原点延续到结尾；单位长度示意淡出 | 每相邻整数点距离相同，0、1 间 Brace 对应一单位 |
| 6 数的对应 | 所有有理数都可对应数轴点，数轴还包含无理数点 | 2.5、-1.5、1/2 用同一 `number_line.n2p(value)` 生成；-3/2=-1.5 只在同一位置提示；√2 单独出现于 1 和 2 之间 | 例点及公式结束后淡出 | 不宣称数轴点只对应有理数，不在 -1.5 重复创建两个含义冲突的点 |
| 7 比较大小 | 左值小于右值：-1<2，-3<-2 | 点、箭头和不等式共用同一组数据；移动点从 -3 到 3，无逐帧 LaTeX 重新编译 | 每个例子单独清理；移动点复用对象，结束后清理 | 箭头方向和公式一致、目标点在可见轴域 |
| 8 总结 | 三要素、有理数可表示、数轴右大左小 | 总结文字与仍显示的数轴相互印证 | 最后淡出数轴、刻度、原点、作者 | 片尾不存在残留临时对象 |

## 修复依据与状态

- 数学纠正：原 `description.json` 的“数轴上的点与有理数一一对应”不成立；与源代码“每个点对应一个有理数”同步更正。例子 √2 的位置由 `number_line.n2p(sqrt(2))` 给出。
- 原脚本曾在替换真实数轴之前先显示新轴刻度；新脚本先替换轴，再构造依赖坐标的标记。原动态标签使用 `always_redraw(MathTex(...))`，已改用同一 Dot 的移动动画。
- 数学及 AST 检查：运行 `python -m unittest discover -s '初中/六年级/第二学期/第五章-有理数/003数轴' -p test_number_line_lesson.py -v`；语法：`python -m py_compile '初中/六年级/第二学期/第五章-有理数/003数轴/number_line_lesson.py'`。
- Manim 审计、实际低清渲染、逐镜关键帧、字体与 TeX、ffprobe、音轨检查：**待执行**。不可仅依据已有 MP4 或静态 CI 将其标记为 render_verified；未经授权不覆盖已有成片。
